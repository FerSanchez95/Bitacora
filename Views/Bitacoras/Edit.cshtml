@model Bitacora.Models.ModeloBitacora

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ModeloBitacora</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="BitacoraId" />
            <div class="form-group">
                <label asp-for="NombreDeBitacora" class="control-label"></label>
                <input asp-for="NombreDeBitacora" class="form-control" />
                <span asp-validation-for="NombreDeBitacora" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Volver</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
